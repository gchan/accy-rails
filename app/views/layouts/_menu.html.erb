<nav class="navbar navbar-default" role="navigation">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Accy', root_url, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">

        <%= dropdown accounts_path do %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            Accounts<b class="caret"></b>
          </a>

          <ul class="dropdown-menu">
            <%= nav_action_link "All Accounts", accounts_path %>
            <li class="divider"></li>
            <% %w(assets liabilities equity expenses revenue).each do |account_type| %>
              <%= nav_action_link account_type.titleize, send("#{account_type}_accounts_path") %>
            <% end %>
          </ul>
        <% end %>

        <%= dropdown entries_path do %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            Journal Entries<b class="caret"></b>
          </a>

          <ul class="dropdown-menu">
            <%= nav_action_link "All Entries", entries_path %>
            <li class="divider"></li>
            <%= nav_action_link "New Entry", new_entry_path %>
          </ul>
        <% end %>

      </ul>

    </div>
  </div>
</nav>